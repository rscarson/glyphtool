[package]
default-run = "glyphtool"
name = "glyphtool"
version = "0.1.0"
edition = "2021"

[dependencies]
manager = { path = "../manager" }
clap = { version = "4.5.30", features = ["derive"] }
